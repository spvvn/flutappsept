# Simple Calculator

A single-page Flutter calculator for basic addition, subtraction,
multiplication, and division.

## Tests

The widget test suite covers the four core calculator operations.

## Run locally

Run `flutter pub get`, then start the app with `flutter run`.
